<div class="container">
  <%= render @director %>

  <div>
    <%= link_to "Edit this director", edit_director_path(@director) %>
    <%= render "likes/form", like: Like.new(likeable: @director) %>
    <%= button_to "Destroy this director", @director, method: :delete %>
  </div>
</div>
